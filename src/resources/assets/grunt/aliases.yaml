default:
    - jshint
    - 'build'
  
build:
    - concurrent:dist
    - modernizr:dist
    - requirejs:dist
    - combine_mq:dist
    - autoprefixer:dist
    - cssmin:dist

server:
    - concurrent:server
    - browserSync
    - watch

serve:
    - server
