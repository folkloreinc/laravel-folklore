<?php

namespace {{ namespace }};

use {{ namespacedModel }} as {{ modelAlias }};
use {{ namespacedResource }} as {{ resourceAlias }};

class {{ class }} implements {{ resourceAlias }}
{
    protected $model;

    public function __construct({{ modelAlias }} $model)
    {
        $this->model = $model;
    }

    public function id(): string
    {
        return $this->model->id;
    }
}
